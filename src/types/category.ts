export type CategoryItem = {
  createDate?: Date,
  createdBy?: string,
  id?: Number,
  img?: string,
  listAticles?: Array<any>,
  modifierBy?: string,
  modifierDate?: Date,
  slug?: string,
  title?: string
};
export type TypeCategory = {
  createDate: Date,
  createdBy: string,
  id: Number,
  img: string,
  listAticles: Array<any>,
  modifierBy: string,
  modifierDate: Date,
  slug: string,
  title: string
}