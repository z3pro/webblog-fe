import { Avatar } from "@mui/material";
import logo from '~/assets/images/logo.png';
const Logo = () => {

    return (
        <img alt="logo" src={ logo} width={110} loading="lazy"/>
    );
}
export default Logo;